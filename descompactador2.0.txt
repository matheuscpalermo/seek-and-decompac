#DEFININDO FUNÇÃO

seek_and_decompac(diretorio)
	{

	for arquivo in find diretorio -name "*.tar.gz"
		do
			tar -xvzf $arquivo
			rmv $arquivo
		done
	for arquivo in # TESTAR TRÊS OPÇÕES: (find diretorio -type d "*") ou (-t list) ou (ls-d*/)
		do 
			simple_decompac($arquivo)
		done
	}		
